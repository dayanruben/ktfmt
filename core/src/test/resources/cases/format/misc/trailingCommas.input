// MAX_WIDTH 26
// TRAILING_COMMA_STRATEGY NONE

fun main() {
  val (
      x: Int,
  ) = foo()
  val (
      x: Int,
      y: Int,
  ) = foo()

  val (
      x: Int,
  ) = foo(
      blablablablFoobar,
      alskdjasld)

  val (
      x: Int,
      y: Int,
  ) = foo(
      blablablablFoobar,
      asldkalsd)

  a[
      0,
  ] = 43
  a[
      0,
      1,
  ] = 43

  [
      0,
  ]
  [
      0,
      1,
  ]

  when (foo) {
    'x',
    -> 43
    'x',
    'y',
    -> 43
    'x',
    'y',
    'z',
    'w',
    'a',
    'b',
    -> 43
  }

  try {
    //
  } catch (e: Error,) {
    //
  }
}