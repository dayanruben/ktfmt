// MAX_WIDTH 35
// TRAILING_COMMA_STRATEGY NONE
// BLOCK_INDENT 4
// CONTINUATION_INDENT 4

fun f() {
    for (child in
        node.next.next.next.next
            .data()) {
        println(child)
    }
}
