// MAX_WIDTH 33

fun f() {
  val fn = scope.launch {
    doThing()
    doAnother()
  }
}

fun g() {
  val longVariableName =
      scope.launch {
        doThing()
        doAnother()
      }
}

fun h() = scope.launch {
  doThing()
  doAnother()
}

fun longFunctionName() =
    scope.launch {
      doThing()
      doAnother()
    }

fun j() {
  x = scope.launch {
    doThing()
    doAnother()
  }
}

fun k() {
  longVariableName =
      scope.launch {
        doThing()
        doAnother()
      }
}

fun l() {
  val fn = scope?.launch {
    doThing()
    doAnother()
  }
}

fun m() {
  val longVariableName =
      scope?.launch {
        doThing()
        doAnother()
      }
}
