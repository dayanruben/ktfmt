class Something {
  @field:[Inject Named("WEB_VIEW")]
  internal lateinit var httpClient: OkHttpClient

  @field:[Inject Named("WEB_VIEW")]
  var httpClient: OkHttpClient

  @Px
  @field:[Inject Named("WEB_VIEW")]
  var httpClient: OkHttpClient
}
