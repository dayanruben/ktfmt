// MAX_WIDTH 40
fun Int.foo() =
    when (this) {
      0 -> "zero"
      else -> "other"
    }

fun foo() =
    when (veryLongSubjectNameThatDoesNotFit) {
      0 -> "zero"
      else -> "other"
    }
